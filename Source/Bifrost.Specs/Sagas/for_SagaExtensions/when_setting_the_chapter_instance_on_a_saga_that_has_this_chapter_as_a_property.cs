using Bifrost.Sagas;
using Machine.Specifications;

namespace Bifrost.Specs.Sagas.for_SagaExtensions
{
    public class when_setting_the_chapter_instance_on_a_saga_that_has_this_chapter_as_a_property
    {
        //static SagaWithOneChapterProperty saga;
        //static SimpleChapter chapter;

        //Establish context = () =>
        //                        {
        //                            saga = new SagaWithOneChapterProperty();
        //                            chapter = new SimpleChapter();
        //                        };

        //Because of = () => saga.SetChapterInstance(chapter);

        //It should_have_set_the_chapter_instance_property = () => saga.GetSimple().ShouldEqual(chapter);
    }
}