Bifrost.namespace("Bifrost.tasks", {
    HttpPostTask: Bifrost.tasks.HttpTask.extend(function () {
        /// <summary>Represents a task that can perform a Http Post request</summary>
        var self = this;

        this.execute = function () {
        };
    })
});