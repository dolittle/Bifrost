Bifrost.namespace("Bifrost.tasks", {
    HttpTask: Bifrost.tasks.Task.extend(function () {
        /// <summary>Represents a task that perform Http Requests</summary>
        var self = this;

        this.execute = function () {

        };
    })
});