Bifrost.namespace("Bifrost.tasks", {
    HttpGetTask: Bifrost.tasks.HttpTask.extend(function () {
        /// <summary>Represents a task that can perform Http Get requests</summary>
        var self = this;

        this.execute = function () {
        };
    })
});