Bifrost.namespace("Bifrost.routing");
Bifrost.routing.Route = (function() {
	function Route(options) {
		
	}
	
	return {
		create: function(options) {
			var route = new Route(options);
			return route;
		}
	}	
})();