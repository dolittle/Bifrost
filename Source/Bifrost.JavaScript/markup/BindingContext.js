Bifrost.namespace("Bifrost.markup", {
    BindingContext: Bifrost.Type.extend(function () {
        this.parent = null;
        this.current = null;

        this.changed = function () {

        };
    })
});